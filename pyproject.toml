[project]
name = "automediaorganizer"
version = "0.1.0"
description = "Automated Media Organizer for media based on config files"
requires-python = ">=3.12"
dependencies = [
    "pymysql>=1.1.2",
    "requests==2.32.5",
    "schedule>=1.2.2",
]
